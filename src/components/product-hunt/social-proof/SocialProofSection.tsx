
import React from "react";

interface SocialProofSectionProps {
  initialLoad?: boolean;
}

export const SocialProofSection: React.FC<SocialProofSectionProps> = () => {
  // Returning null so the section doesn't render at all
  return null;
};
